# 三件早期作品：源文件检查

本页保留导入时的源文件检查。随后两张静图已完成 [本机重新渲染与计时](../docs/showcase-measurements.md#两张静图本机重新渲染)，新记录与下面的历史检查分开保存。

检查日期：2026-09-08。本次使用 Blender 5.2.1 LTS，以后台、恢复默认设置、禁用自动执行的方式重新打开三份核心 `.blend`，检查场景结构、素材依赖和文件路径。没有运行资料包中的制作脚本或内嵌脚本，没有重新渲染，也没有覆盖源文件；检查前后 SHA256 一致。

| 作品 | 核心 `.blend` 大小 | 对象 / 场景 | 保存的渲染设置 |
|---|---:|---:|---|
| 粉色装置 | 1.75 MiB | 244 / 1 | Cycles，1080 × 1048，100%，128 samples |
| 毛绒兔骑士 | 30.94 MiB | 269 / 1 | Cycles，1000 × 1000，100%，1024 samples |
| 悬浮几何动画 | 4.63 MiB | 830 / 6 | Cycles，720 × 1280，100%，64 samples |

三份工程均成功打开，未发现外链库、外部贴图、外部字体、外部缓存、视频文件依赖或文本块；所检查的数据块也未发现驱动表达式。兔骑士的毛发粒子系统和曲线几何保存在工程中，未启用外部毛发缓存。

两份静图工程保存的时间轴范围均为 1–250 帧、24 fps；这是工程设置，不表示它们附带已完成的动画。两张 1254 × 1254 展示终图经过生成式图像精修，未写回 `.blend`；可编辑底稿直接渲染不能保证复现精修终图。

动画工程包含五套各 180 帧的三维场景，以及一条 1–900 帧、30 fps 的总剪辑时间线，对应 30 秒。剪辑使用工程内的场景条带；音轨已打包，内嵌数据为 244,985 字节。路径列表中仍能看到音轨的历史来源记录，但打开工程不需要原电脑上的音频文件。五套分场景 `.blend` 的来源记录属于追加数据的弱引用；当前数据已保存在主工程内，未链接外部 `.blend` 库。

工程仍保留历史渲染输出位置。搬到自己的电脑后，先设置一个可写的输出目录再渲染；历史输出位置不是输入素材依赖。为保留原始文件，本次未改写这些路径。

以上结论限定于重开、结构与依赖检查，没有重新验证全部动画帧、音轨试听、渲染耗时或与既有成品的逐像素一致性。三份核心文件都小于 100 MiB；兔骑士工程大于 25 MiB，应按仓库的大文件发布方式上传。
